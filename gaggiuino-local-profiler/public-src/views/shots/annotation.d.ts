// Type shim for the not-yet-migrated views/shots/annotation.js (TypeScript
// migration package A4, #1113). Only the symbols consumed by components/ are
// declared; delete this file once the module is converted.
export function loadShotDefaults(): Promise<void>;
export function loadDrinkMenu(): Promise<void>;
